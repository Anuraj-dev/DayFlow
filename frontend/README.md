# dayflow-web

Vue 3 + TypeScript client. Run the full stack from the repository root with `docker compose up --build`. Commands for this package are in `package.json`. Product rules are in `../AGENTS.md`.
